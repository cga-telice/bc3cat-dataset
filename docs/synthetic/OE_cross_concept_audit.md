# Cross-concept collision audit (Guard #1)

Zero *introduced* collisions = pass. *Inherited* collisions are leaves the source catalogue already shares across concepts (a ground-truth property, not a synthesis defect).

## Corpus-level (5038 items)

- introduced collisions (guard failures): **0**
- inherited collisions (baseline catalogue duplicates): 93
- inherited concept pairs (top): OED020$≡OED170$ (93)

## Template-level (exhaustive over all leaves)

- introduced collisions (guard failures): **0**
- inherited collisions (baseline catalogue duplicates): 22680
- inherited concept pairs (top): OED020$≡OED170$ (22680)
